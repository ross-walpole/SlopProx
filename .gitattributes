# Normalise line endings to LF in the repo
* text=auto eol=lf

# Binaries — never touch
*.onnx  filter=lfs diff=lfs merge=lfs -text
*.png   binary
*.jpg   binary
*.jpeg  binary
*.ico   binary
*.woff  binary
*.woff2 binary
*.exe   binary
*.dll   binary
